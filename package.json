{
  "name": "purple_ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "esbuild app.ts --bundle --outfile=app.js --watch=forever & nodemon app.js",
    "build": "esbuild app.ts --bundle --outfile=app.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.19.4",
    "nodemon": "^3.0.1"
  }
}
